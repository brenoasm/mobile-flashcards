export const getActualCard = ({ cards, totalAnsweredCards }) => cards[totalAnsweredCards];
export const getTotalCards = ({ totalCards }) => totalCards;
export const getTotalAnsweredCards = ({ totalAnsweredCards }) =>
  totalAnsweredCards;
export const getShowAnswer = ({ showAnswer }) => showAnswer;
export const getTotalCorrectAnswers = ({ totalCorrectAnswers }) => totalCorrectAnswers;
export const getTotalIncorrectAnswers = ({ totalIncorrectAnswers }) => totalIncorrectAnswers;
